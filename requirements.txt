Flask>=3.0.0,<3.1.0
flask-cors>=4.0.0,<5.0.0
numpy>=1.26.4,<3.0.0
# If tflite-runtime fails on your Render stack, comment it and use tensorflow instead
# (the app falls back to tf.lite.Interpreter automatically)
tflite-runtime>=2.14.0,<3.0.0
# Production WSGI server
gunicorn>=21.2.0,<22.0.0
